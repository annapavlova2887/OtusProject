package pages.events;
import org.openqa.selenium.WebDriver;
import org.openqa.selenium.WebElement;

public class EventCard extends EventsBasePage {
    WebElement card;
    public EventCard(WebDriver driver) {
        super(driver);
    }
}
